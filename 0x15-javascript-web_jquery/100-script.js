/**
 * This script updates the text color of the HTML tag HEADER to red (#FF0000)
 */
window.addEventListener('load', () => {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
});
